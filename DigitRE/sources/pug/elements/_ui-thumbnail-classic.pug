mixin thumbnail-classic(obj)
  .thumbnail-classic
    a(href=obj.link ? obj.link : '#'): +jpg(obj.img)
    .caption
      h5: a(href=obj.link ? obj.link : '#').thumbnail-classic-title=obj.title
      p=obj.text
